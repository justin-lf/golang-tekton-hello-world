name: 'PR'

on:
  pull_request_review:
    types: [submitted]

jobs:
  submitted:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This comment submitted"
  approved:
    if: github.event.review.state = 'approved'
    runs-on: ubuntu-latest
    steps:
      - run: echo "This PR was approved"
