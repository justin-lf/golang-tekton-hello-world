name: 'Main1'

on:
  pull_request_review
  
permissions:
  contents: read
env:
  ACTOR: ${{github.actor}}
  
jobs:
  terraform:
    runs-on: ubuntu-latest
  
    steps:
    - name: Setup Git    
      run: |
        env
